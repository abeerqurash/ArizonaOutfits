<header><span>Danger zone</span><h3>Delete account</h3><p>This permanently removes your account. Completed order records may be retained where legally required.</p></header>
<details class="customer-delete-account"><summary>Delete my account</summary><form method="POST" action="{{ route('profile.destroy') }}">@csrf @method('DELETE')<p>Enter your password to confirm permanent account deletion.</p><label class="customer-field"><span>Password</span><input name="password" type="password" autocomplete="current-password">@foreach($errors->userDeletion->get('password') as $message)<small>{{ $message }}</small>@endforeach</label><button type="submit">Permanently delete account</button></form></details>
